package com.aspect.compose.inspector.ui

import androidx.compose.runtime.Composable
import com.aspect.compose.inspector.model.InspectorNode

/**
 * Visualizes recomposition frequency using color-coded indicators.
 * Green = low, Yellow = medium, Red = high recomposition count.
 */
@Composable
fun RecompositionHeatmap(
    nodes: List<InspectorNode>
) {
    // TODO: Implement recomposition heatmap (P2)
}
